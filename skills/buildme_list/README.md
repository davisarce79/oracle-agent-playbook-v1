# Telegram /buildme-list Skill

Commands:
- `/buildme-list` — list recent Build Me This scaffolds
- `/buildme-list get <folder>` — create a zip archive of the scaffold

Useful for retrieving generated projects.
